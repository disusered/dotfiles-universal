linux[whoami.distro^="Arch"]:
  installs:
    cmd: |-
      yay -S --noconfirm --needed hyprshot
      mkdir -p ~/Pictures/Screenshots
    depends:
      - /lib/slurp
      - /arch/hyprpicker
  links:
    hyprshot.conf: ~/.config/hypr/conf.d/hyprshot.conf
