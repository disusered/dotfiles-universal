linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S --noconfirm --needed chatgpt-desktop
    depends:
      - /tools/yay
      - /arch/kwallet
  links:
    chatgpt-flags.conf: ~/.config/chatgpt-flags.conf
