linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S --noconfirm hyprpicker
  depends:
    - /lib/wl-clipboard
  links:
    hyprpicker.conf: ~/.config/hypr/conf.d/hyprpicker.conf
    color-picker.sh: ~/.local/bin/pick-color
