linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      sudo locale-gen
      sudo pacman -S --noconfirm ttf-liberation
    depends:
      - /arch/hyprland
      - /lib/networkmanager/
      - /lib/avahi/
      - /lib/pipewire/
