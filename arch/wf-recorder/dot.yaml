linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed wf-recorder
    depends:
      - /lib/slurp
      - /lib/uwsm
      - /arch/hyprland
      - /tools/ffmpeg
      - /tools/rofi
      - /tools/jq
  links:
    wf-recorder.lua: ~/.config/hypr/conf.d/wf-recorder.lua
    recorder.sh: ~/.config/hypr/scripts/recorder.sh
