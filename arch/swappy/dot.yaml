linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed swappy
    depends:
      - /arch/hyprshot
  links:
    config: ~/.config/swappy/config
    swappy.conf: ~/.config/hypr/conf.d/swappy.conf
