linux[whoami.distro^="Arch"]:
  # cfg leds uses the Keychron QMK/VIA Raw HID interface.
  installs:
    cmd: |
      sudo cp ~/.dotfiles/arch/keychron/keychron.rules /etc/udev/rules.d/
      sudo udevadm control --reload
      sudo udevadm trigger --subsystem-match=hidraw
