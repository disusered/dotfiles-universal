linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      if ! pacman -Qi hypridle &>/dev/null; then
        yay -S --noconfirm --needed hypridle
      fi
      systemctl --user enable --now hypridle.service
  links:
    hypridle.conf: ~/.config/hypr/hypridle.conf
