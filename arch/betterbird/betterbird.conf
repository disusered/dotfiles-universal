# vi: ft=hyprlang

# Keep mail available in the background; Betterbird's own profile preference
# controls whether startup lands in the tray without showing a window.
exec-once = app2unit -- betterbird
