linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      yay -S --noconfirm aur/bruno
      npm i -g @usebruno/cli
    depends:
      - /languages/javascript/node/latest
  links:
    bruno.sh: ~/.local/bin/bruno-partial-import
