# vi: ft=hyprlang

# Hotline Miami — gamescope performance rules
# The global gamescope_global rule in steam.conf handles float/center/no_bar.
# This adds fullscreen and latency optimizations.
windowrule {
  name = hotline_miami
  match:class = gamescope
  fullscreen = true
  immediate = true
  animation = none
}
