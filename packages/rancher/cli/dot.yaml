windows:
  installs:
    cmd: scoop install main/rancher-cli
    depends:
      - /packages/scoop
linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S rancher-cli-bin
    depends:
      - /tools/yay
