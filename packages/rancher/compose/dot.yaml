windows:
  installs:
    cmd: scoop install main/rancher-compose
    depends:
      - /packages/scoop
linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S rancher-compose-bin
    depends:
      - /tools/yay
