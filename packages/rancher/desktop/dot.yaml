windows:
  installs:
    cmd: scoop install extras/rancher-desktop
    depends:
      - /packages/scoop
      - /packages/rancher/cli
      - /packages/rancher/compose
linux:
  # TODO: Install when not in WSL
  installs: false
