---
description: Remove AI code slop while preserving intended behavior
---

Use the `slop-clean` skill. Its canonical source is
`~/.dotfiles/ai/agent-skills/slop-clean/SKILL.md`. Keep cleanup within the
requested branch diff and preserve intended behavior and necessary checks.
