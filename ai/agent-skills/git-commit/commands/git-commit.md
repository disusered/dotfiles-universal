---
name: git-commit
description: Prepare and create logical signed commits using the shared git-commit skill
---

Use the `git-commit` skill. Its canonical source is
`~/.dotfiles/ai/agent-skills/git-commit/SKILL.md`. Follow the repository's
commit style and VCS workflow, including its jj-colocated and signing rules.
Commit only the requested changes; this command does not request a push.
