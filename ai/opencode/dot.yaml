global:
  links:
    opencode.jsonc: ~/.config/opencode/opencode.jsonc
windows:
  installs:
    cmd: scoop install extras/opencode
    depends:
      - /packages/scoop
linux:
  installs:
    cmd: brew install sst/tap/opencode
    depends:
      - /packages/homebrew
