#!/usr/bin/env bash
set -euo pipefail

stitch_dir="${CODEX_STITCH_MCP_DIR:-$HOME/.codex/mcp-servers/stitch}"
template_file="$stitch_dir/env.tpl"
env_file="$stitch_dir/env.local"

mkdir -p "$stitch_dir"
op inject -f -i "$template_file" -o "$env_file"
chmod 600 "$env_file"
echo "Rendered $env_file"
