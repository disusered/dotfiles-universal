global:
  installs:
    cmd: command -v gemini &>/dev/null || npm install -g @google/gemini-cli
    depends:
      - /languages/javascript/node/latest
  links:
    gemini.conf: {{ dirs.base.config }}/hypr/conf.d/gemini.conf
