linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      yay -S --noconfirm --needed icu74 dolt
      command -v bd &>/dev/null || go install github.com/steveyegge/beads/cmd/bd@latest
  links:
    jira-to-md.py: ~/.local/bin/jira-to-md
  depends:
    - /languages/go/latest
