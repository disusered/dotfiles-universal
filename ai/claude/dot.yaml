global:
  installs:
    cmd: command -v claude &>/dev/null || curl -fsSL https://claude.ai/install.sh | bash
    depends:
      - /ai/agent-skills
      - /tools/hyprspace
  links:
    skills: ~/.claude/skills/
    rules: ~/.claude/rules/
    CLAUDE.md: ~/.claude/CLAUDE.md
linux|darwin:
  links:
    settings.json: ~/.claude/settings.json
    file-suggestion.sh: ~/.claude/file-suggestion.sh
    claude.conf: {{ dirs.base.config }}/hypr/conf.d/claude.conf
windows:
  links:
    settings_win.json: ~/.claude/settings.json
