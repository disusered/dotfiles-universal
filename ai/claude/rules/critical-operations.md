# Critical Operations

## File Listing

**ALWAYS use `/bin/ls`** - `ls` is aliased to `exa` in the shell.

```bash
/bin/ls -la   # correct
ls -la        # wrong - uses exa
```

## Commit Messages

- Limit to 80 characters
- No footers, signatures, or tool attribution
