linux[whoami.distro^="Arch"]:
  installs:
    cmd: command -v ouroboros &>/dev/null || uv tool install --with litellm ouroboros-ai
    depends:
      - /languages/python/uv
  links:
    ouroboros-bridge.ts: ~/.config/opencode/plugins/ouroboros-bridge/ouroboros-bridge.ts
