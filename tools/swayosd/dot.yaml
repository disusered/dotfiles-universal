linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm swayosd
    depends:
      - /lib/pipewire
  links:
    style.css: ~/.config/swayosd/style.css
    sway.conf: ~/.config/hypr/conf.d/sway.conf
