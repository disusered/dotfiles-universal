linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed github-cli
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: brew install gh
    depends:
      - /packages/homebrew
windows:
  installs:
    cmd: scoop install main/gh
    depends:
      - /packages/scoop
