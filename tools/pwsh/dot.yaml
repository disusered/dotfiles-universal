linux|darwin:
  installs: false
# FIXME: Use winget to install powershell
windows:
  installs:
    cmd: pwsh -File $HOME/.dotfiles/tools/pwsh/install.ps1;
    depends:
      - /packages/scoop
  links:
    profile.ps1: ~/Documents/PowerShell/profile.ps1
