linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed httpie
    depends:
      - /tools/jq
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: brew install httpie
    depends:
      - /packages/homebrew
      - /tools/jq
windows:
  installs: false
