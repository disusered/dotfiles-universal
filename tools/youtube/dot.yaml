linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S --noconfirm --needed ytcast
    depends:
      - /tools/fzf
      - /tools/jq
      - /tools/curl
  links:
    ytsearch.sh: ~/.local/bin/ytsearch
