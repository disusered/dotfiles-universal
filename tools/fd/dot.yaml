windows:
  installs:
    cmd: scoop install fd
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: command -v fd &>/dev/null || sudo dnf install fd-find -y
linux[whoami.distro^="Arch"]:
  installs:
    cmd: yes | sudo pacman -S fd
