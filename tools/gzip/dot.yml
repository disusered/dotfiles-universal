windows:
  installs:
    cmd: scoop install gzip
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: command -v gzip &>/dev/null || sudo dnf install gzip -y
linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed gzip

