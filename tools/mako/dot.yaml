linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm mako
    depends:
      - /arch/hyprland
  links:
    config: ~/.config/mako/config
    mako.conf: ~/.config/hypr/conf.d/mako.conf
