linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed mako
    depends:
      - /arch/hyprland
  links:
    config: ~/.config/mako/config
    mako.lua: ~/.config/hypr/conf.d/mako.lua
