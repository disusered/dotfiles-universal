windows:
  installs:
    cmd: >
      scoop bucket add nerd-fonts;
      scoop install CascadiaCode-NF-Mono;
      scoop install FiraCode;
      scoop install hack-font;
      scoop install Hasklig
    depends:
      - /packages/scoop
linux[whoami.distro^="Arch"]|darwin:
  installs:
    cmd: >
      sudo pacman -S --noconfirm ttf-fira-code ttf-cascadia-code-nerd otf-hasklig-nerd ttf-hack
    depends:
      - /lib/arch
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: >
      sudo dnf install -y ttf-fira-code cascadia-code-nf-fonts
    # TODO: Manually install Hasklig and hack-font

