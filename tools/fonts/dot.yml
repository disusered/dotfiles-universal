windows:
  installs:
    cmd: >
      scoop bucket add nerd-fonts;
      scoop install CascadiaCode-NF-Mono;
      scoop install FiraCode;
      scoop install hack-font;
      scoop install Hasklig
    depends:
      - /packages/scoop
# TODO: Add required fonts for platform-specific Wezterm
linux|darwin:
  installs: false
