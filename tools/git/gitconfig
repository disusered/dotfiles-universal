[user]
  name = Carlos Rosquillas
  signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIN0ymLThQeG/B8GhYqrOUdjr2yydERDzP94yU9LT9o+N
  email = crosquillas@gmail.com
[gpg]
  format = ssh
[gpg "ssh"]
  program = "/opt/1Password/op-ssh-sign"
  allowedSignersFile = ~/.ssh/allowed_signers
[commit]
  gpgsign = true
[advice]
  statusHints = true
[color]
  ui = true
  status = always
[core]
  pager = delta
  quotepath = false
  editor = nvim
  autocrlf = input
  trustctime = false
  excludesfile = ~/.gitignore_global
  attributesFile = ~/.config/git/attributes
[pretty]
  basic = %C(brightyellow bold)%h%C(reset) %C(auto)%d%C(reset) %s
  extended = %C(brightyellow bold)%h%C(reset) %C(green bold)%aN%C(reset) %C(auto)%d%C(reset) %s %C(8)(%cr)%C(reset)
  reflog = %C(brightyellow bold)%h%C(reset)  %C(green bold)%gd%C(reset) %gs %C(8)(%cr)%C(reset)
[alias]
  l = log --color --pretty=extended --graph --date=relative
  la = log --color --pretty=extended --graph --date=relative --branches --tags --remotes
  s = status --short --branch
  b = branch
  bc = checkout -b
  c = commit --verbose
  ca = commit --verbose --amend
  cm = commit --message
  g = grep --break --heading --line-number
  a = add
  p = push
  pf = push --force
  d = diff
  dt = difftool
  m = merge
  mt = mergetool
  f = fetch
  fm = pull
  co = checkout
[grep]
  lineNumber = true
[branch]
  autosetuprebase = always
[rerere]
  enabled = true
[status]
  submoduleSummary = true
[init]
  defaultBranch = main
  trustExitCode = true
[pull]
  rebase = true
[interactive]
  diffFilter = delta --color-only
[diff "image"]
  command = sh -c 'kitten diff "$2" "$5"' -
[diff]
  tool = kitty
[difftool]
  prompt = false
  trustExitCode = true
[difftool "kitty"]
  cmd = kitten diff $LOCAL $REMOTE
[delta]
  features = catppuccin-mocha shell links
[delta "links"]
  hyperlinks = true
  hyperlinks-file-link-format = "file://{path}#{line}"
[delta "shell"]
  navigate = true  # use n and N to move between diff sections
  side-by-side = true
  pager = less -RXF
[delta "lazygit"]
  features = catppuccin-mocha
  navigate = false
  side-by-side = false
  paging = never
[delta "catppuccin-mocha"] # https://github.com/catppuccin/delta
  true-color = always
  blame-palette = "#1e1e2e #181825 #11111b #313244 #45475a"
  commit-decoration-style = "#6c7086" bold box ul
  dark = true
  file-decoration-style = "#6c7086"
  file-style = "#cdd6f4"
  hunk-header-decoration-style = "#6c7086" box ul
  hunk-header-file-style = bold
  hunk-header-line-number-style = bold "#a6adc8"
  hunk-header-style = file line-number syntax
  line-numbers-left-style = "#6c7086"
  line-numbers-minus-style = bold "#80748b"
  line-numbers-plus-style = bold "#75818a"
  line-numbers-right-style = "#6c7086"
  line-numbers-zero-style = "#6c7086"
  minus-emph-style = bold syntax "#53394d"
  minus-style = syntax "#33293a"
  plus-emph-style = bold syntax "#404f4b"
  plus-style = syntax "#2c323a"
  map-styles = \
    bold purple => syntax "#494060", \
    bold blue => syntax "#394461", \
    bold cyan => syntax "#3c4f58", \
    bold yellow => syntax "#554f4e"
# Should match the name of the bat theme
# bat --list-themes
  syntax-theme = Catppuccin Mocha
