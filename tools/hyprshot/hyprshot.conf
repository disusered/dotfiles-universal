# vi: ft=hyprlang

###################
### KEYBINDINGS ###
###################

$mainMod = SUPER

# Take screenshot of window
bind = $mainMod, C, exec, hyprshot -m window

# Take screenshot of region
bind = $mainMod SHIFT, C, exec, hyprshot -m region
