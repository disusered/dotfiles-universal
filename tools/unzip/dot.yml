windows:
  installs:
    cmd: scoop install unzip
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: command -v unzip &>/dev/null || sudo dnf install unzip -y
linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm zip unzip
