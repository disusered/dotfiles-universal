windows:
  installs:
    cmd: scoop install fzf
    depends:
      - /packages/scoop
linux:
  installs:
    cmd: brew install fzf
    depends:
      - /packages/homebrew
      - /tools/zsh
  links:
    fzf.zsh: ~/.config/zsh/0170_fzf.zsh
