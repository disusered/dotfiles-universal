windows:
  installs:
    cmd: scoop install delta
    depends:
      - /packages/scoop
linux:
  installs:
    cmd: brew install delta
    depends:
      - /packages/homebrew
      - /tools/bat
      - /tools/git
  links:
    delta.zsh: ~/.config/zsh/0025_delta.zsh
