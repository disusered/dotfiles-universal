windows:
  installs:
    false
    # TODO: Install from Github
    # https://github.com/vi/websocat/releases/download/v4.0.0-alpha2/websocat.x86_64-pc-windows-gnu.exe
    # cmd: scoop install main/grpcurl
linux|darwin:
  installs:
    cmd: brew install websocat
    depends:
      - /packages/homebrew
