linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      sudo pacman -S --noconfirm --needed rofi-wayland
      yay -S --noconfirm --needed rofi-calc
  links:
    config.rasi: ~/.config/rofi/config.rasi
    rofi.lua: ~/.config/hypr/conf.d/rofi.lua
