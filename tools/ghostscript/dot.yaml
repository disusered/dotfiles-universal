windows:
  installs:
    cmd: scoop install ghostscript
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: sudo dnf install ghostscript -y
linux[whoami.distro^="Arch"]:
  installs:
      cmd: yes | sudo pacman -S ghostscript
