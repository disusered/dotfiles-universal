windows:
  installs: false
linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed xdotool
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: command -v xdotool &>/dev/null || sudo dnf install -y xdotool
