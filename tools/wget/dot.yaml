windows:
  installs:
    cmd: scoop install wget
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: command -v wget &>/dev/null || sudo dnf install wget -y
linux[whoami.distro^="Arch"]:
  installs:
    cmd: yes | sudo pacman -S wget
