linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      sudo pacman -S --noconfirm --needed rtorrent
  links:
    rtorrent.rc: ~/.rtorrent.rc
    refresh-trackers.py: ~/.local/bin/rtorrent-refresh-trackers
