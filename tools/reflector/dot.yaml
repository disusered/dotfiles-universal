linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      if ! command -v reflector &>/dev/null; then
        yay -S --noconfirm --needed reflector
        sudo reflector --latest 20 --sort rate -l 20 --save /etc/pacman.d/mirrorlist
      fi
