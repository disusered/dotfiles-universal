# TODO: Windows
# TODO: Arch
linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      curl -sS \
        # TODO: Allow this to be repeated, it fails with git clone at the moment
        https://downloads.1password.com/linux/keys/1password.asc | \
        gpg --import && \
        git clone https://aur.archlinux.org/1password.git ~/.1password && \
        cd ~/.1password && yes | makepkg -si
    depends:
      - /lib/pipewire
