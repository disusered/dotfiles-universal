linux|darwin:
  installs:
    cmd: command -v pi &>/dev/null || npm install -g --ignore-scripts @earendil-works/pi-coding-agent
    depends:
      - /languages/javascript/node/latest
  links:
    scripts/brain-pi: ~/.local/bin/brain-pi
    scripts/brain-pi-write: ~/.local/bin/brain-pi-write
