linux[whoami.distro^="Arch"]:
  installs:
    cmd: |
      if [ ! -d ~/.local/share/catppuccin-kde ]; then
        git clone --depth=1 https://github.com/catppuccin/kde ~/.local/share/catppuccin-kde
        cd ~/.local/share/catppuccin-kde && ./install.sh
      fi
    depends:
      - /lib/qt
