/* vi: ft=css */
/* Catppuccin {{ flavor | capitalize }} */

* {
  font-family: "Noto Sans Regular", sans-serif;
  font-size: 13px;
  min-height: 0;
}

window#waybar {
  background-color: {{ base_hex }};
  color: {{ text_hex }};
}

#workspaces {
  background-color: {{ surface0_hex }};
  margin: 5px;
  padding: 0px 5px;
  border-radius: 10px;
}

#workspaces button {
  padding: 0px 15px;
  color: {{ text_hex }};
  border-radius: 10px;
}

#workspaces button.active {
  color: {{ base_hex }};
  background-color: {{ accent_color_hex }};
}

#workspaces button:hover {
  background-color: {{ surface1_hex }};
  color: {{ text_hex }};
}

#tray {
  background-color: {{ surface0_hex }};
  margin: 5px;
  padding: 0px 10px;
  border-radius: 10px;
}

#tray>.passive {
  -gtk-icon-effect: dim;
}

#tray>.needs-attention {
  -gtk-icon-effect: highlight;
  background-color: {{ red_hex }};
}

#clock,
#mpris,
#wireplumber,
#privacy,
#custom-weather,
#custom-powermenu {
  background-color: {{ surface0_hex }};
  color: {{ text_hex }};
  padding: 0px 15px;
  margin: 5px;
  border-radius: 10px;
}

#privacy {
  background-color: {{ red_hex }};
  color: {{ surface0_hex }};
}
