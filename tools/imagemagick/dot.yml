windows:
  installs:
    cmd: scoop install imagemagick
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: sudo dnf install ImageMagick ImageMagick-devel -y
linux[whoami.distro^="Arch"]:
  installs:
      cmd: yes | sudo pacman -S imagemagick
