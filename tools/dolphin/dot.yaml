linux[whoami.distro^="Arch"]:
  installs:
    cmd: yay -S --noconfirm dolphin archlinux-xdg-menu
    depends:
      - /tools/qt6ct
  links:
    env: ~/.config/uwsm/env
    dolphin.conf: ~/.config/hypr/conf.d/dolphin.conf
    kdeglobals: ~/.config/kdeglobals
