linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm --needed terraform
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: brew install terraform
  depends:
    - /packages/homebrew
