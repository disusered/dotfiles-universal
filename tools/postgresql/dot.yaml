global:
  links:
    psqlrc: ~/.psqlrc
windows:
  installs:
    cmd: scoop install main/postgresql
    depends:
      - /packages/scoop
linux:
  installs:
    cmd: sudo dnf install postgresql libpq-devel -y
