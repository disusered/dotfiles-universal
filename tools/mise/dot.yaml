windows:
  installs:
    cmd: >
      pwsh -File $HOME/.dotfiles/tools/mise/install.ps1;
    depends:
      - /packages/scoop
      - /tools/pwsh
  links:
    shims.ps1: ~/.config/powershell/profile.d/shims.ps1
    config.toml: ~/.config/mise/config.toml
