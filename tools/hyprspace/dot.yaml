linux[whoami.distro^="Arch"]:
  installs:
    cmd: cd tools/hyprspace && cargo build --release
  links:
    target/release/hyprspace: ~/.local/bin/hyprspace
    config.toml: ~/.config/hyprspace/config.toml
