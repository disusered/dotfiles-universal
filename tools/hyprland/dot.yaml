linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm hyprland hyprpolkitagent xdg-desktop-portal-hyprland qt5-wayland qt6-wayland
    depends:
      - /tools/jq
      - /lib/pipewire
  links:
    hyprland.conf: ~/.config/hypr/hyprland.conf
    scripts/toggle-monitors.sh: ~/.config/hypr/scripts/toggle-monitors.sh
