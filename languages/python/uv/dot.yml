windows:
  installs:
    cmd: scoop install uv
    depends:
      - /packages/scoop
linux:
  installs:
    cmd: brew install uv
    depends:
      - /packages/homebrew
  links:
    uv.zsh: ~/.config/zsh/0300_uv.zsh
