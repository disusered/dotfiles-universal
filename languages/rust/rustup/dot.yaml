windows:
  installs:
    cmd: >
      pwsh -File $HOME/.dotfiles/languages/rust/rustup/install.ps1;
linux:
  installs:
    cmd: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  links:
    rust.zsh: ~/.config/zsh/0400_rust.zsh
