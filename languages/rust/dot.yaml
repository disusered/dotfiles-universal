global:
  installs:
    cmd: rustup toolchain install stable && { rustup component list --installed 2>/dev/null | grep -q '^rustfmt' || rustup component add rustfmt; }
    depends:
      - /languages/rust/rustup
