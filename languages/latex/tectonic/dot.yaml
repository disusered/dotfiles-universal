windows:
  installs:
    cmd: scoop install tectonic
    depends:
      - /packages/scoop
linux[whoami.distro^="Fedora"]:
  installs:
    cmd: brew install tectonic
    depends:
      - /packages/homebrew
linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm tectonic
