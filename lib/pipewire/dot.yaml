linux[whoami.distro^="Arch"]:
  installs:
    cmd: sudo pacman -S --noconfirm \
      pipewire-alsa pipewire-audio pipewire-pulse gst-plugin-pipewire \
      wireplumber pavucontrol pamixer playerctl
