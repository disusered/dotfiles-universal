context.modules = [
  {
    name = libpipewire-module-raop-sink
    args = {
        # Using the separate ip and port properties from the new docs
        raop.ip              = "192.168.1.64"
        raop.port            = 7000

        raop.name = "Airport Express"
        raop.hostname = "AirPort-Express.local"
        # raop.password = "****"

        raop.encryption.type = "RSA"
        # raop.transport = "udp"

        # raop.audio.codec     = "ALAC"
        # raop.audio.codec = "PCM"

        # audio.format = "S16"
        # audio.rate = 44100
        # audio.channels = 2
        # audio.position = [ FL FR ]

        # The user-friendly name goes in stream.props
        stream.props = {
            node.description = "Stereo (AirPlay)"
        }
    }
  }
]
