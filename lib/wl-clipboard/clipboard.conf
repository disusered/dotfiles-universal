# vi: ft=hyprlang
#
###################
### KEYBINDINGS ###
###################

# Super V to show clipboard history
$mainMod = SUPER
bind = $mainMod, V, exec, cliphist list | app2unit -- rofi -dmenu -display-columns 2 | cliphist decode | wl-copy

exec-once = wl-paste --watch cliphist store
